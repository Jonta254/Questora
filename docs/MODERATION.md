# Moderation

The schema reserves visible, pending, hidden, and removed content states plus reports and blocks. User-facing report, block, appeal, reviewer queues, audit history, service levels, and enforcement are planned and not shipped. Until those exist, user publishing is disabled.
