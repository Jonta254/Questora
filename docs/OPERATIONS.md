# Operations

`/api/config` reports public readiness flags without exposing secrets. Logs must never include access tokens or cookies. Before enabling publishing, add structured request IDs, database health checks, alerting, backups, restore drills, moderation escalation, support ownership, and rollback procedures.
