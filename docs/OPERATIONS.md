# Operations

`/api/ping` reports session configuration and confirms payments are disabled. Logs must never include access tokens or cookies. Before enabling publishing, add structured request IDs, database health checks, alerting, backups, restore drills, moderation escalation, support ownership, and rollback procedures.
