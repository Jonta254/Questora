# Testing

Run `npm run verify` for JavaScript syntax and product-contract tests. Manually verified Home, Ask duplicate hint, Activity, Profile, update-cache behavior, and horizontal overflow at a 350px browser viewport. Exact 320, 360, 390, 412, and 430px captures, real Pi authentication, configured database flows, concurrency, moderation, screen readers, payments, and physical-device behavior are not verified.
